# Devenv-only: trust the plain-HTTP local registry reachable from inside the VM
# at the qemu user-mode network gateway address (host-side registry, see
# test/devenv/README.md). Not part of the homelab package/config lineage.
[[registry]]
location = "10.0.2.2:5000"
insecure = true
